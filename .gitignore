node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Connection presets (contains credentials)
presets.json

# Local query perf instrumentation
perf-log.jsonl

# Private schema metadata — do not commit (this repo is public).
# Lives in local/, which is expected to be its own private git repo.
# Regenerate schema-map.json with your own extractor.
local/
