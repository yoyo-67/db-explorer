node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Connection presets (contains credentials)
presets.json

# Local query perf instrumentation
perf-log.jsonl

# Local table catalog (internal schema — do not commit)
table-catalog.json
fk-overrides.json
