node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Connection presets (contains credentials)
presets.json

# Local query perf instrumentation
perf-log.jsonl

# Internal schema metadata — do not commit (this repo is public).
# Lives in local/, which is its own private git repo, mirroring pycode/local_dev.
# Regenerate with: python3 ~/Work/buildots/pycode/local_dev/schema_map/extract.py
local/
